// Feather disable all
/// @param text
/// @param callback
/// @param [data=undefined]

function UIButton(_text, _callback, _data = undefined)
{
    UIButtonInline(_text, _callback, _data);
    UINewline();
}
